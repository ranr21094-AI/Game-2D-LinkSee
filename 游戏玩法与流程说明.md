# 游戏玩法与流程说明 — 声路·澳门 2D

> 本文件是当前版本的权威玩法总结，分「上篇 · 给玩家/观众」与「下篇 · 给开发」。
> 所有数字与流程均以 `src/game/` 当前源码与 `progress.md`（2026-08-06）为准。
> 运行环境：桌面 Chrome / Edge，Node.js 20+，纯键盘操作。

---

## 上篇 · 给玩家与观众

### 1. 这是什么

**《声路·澳门》** 是一款桌面 Web 像素叙事游戏：你扮演一位低视力出行者，从**拱北口岸**出发，搭乘 **17 路巴士**在白鸽巢下车，穿过旧城，前往**大三巴牌坊**赴一场与老友林伯的约。

城市默认以明亮暖灰呈现。**盲杖敲击会让触碰处的街景短暂恢复完整暖色**；声音、盲杖和你的判断共同组成路线，**游戏不会替你自动寻路**。

三个特色：

- **以「听觉 + 触觉 + 语音」替代视觉** —— 盲杖敲击读材质、驻足聆听辨声向、语音播报全程伴随，全流程无需鼠标。
- **澳门在地叙事** —— 關閘、17 路、白鸽巢旧城、大三巴牌坊，一场雨后的真实出行。
- **两条可选游玩模式** —— 体验模式（触碰留下淡彩记忆）与黑夜模式（未触碰处全黑，更有挑战）。

### 2. 操作

| 按键 | 动作 |
| --- | --- |
| `WASD` / `方向键` | 行走 |
| `Space` | 向当前方向敲击一根盲杖，读取前方与脚下材质 |
| `E` | 互动：交谈、确认物体、乘车、坐下、按铃 |
| `R` | 驻足聆听（1.2 秒），报告附近声音的方向与远近；冷却 2 秒 |
| `Q` | 方向提示：只说出目标方位并短暂画出方向箭头，不画整条路线；冷却约 4.5 秒 |
| `H` | 复述：重复最近触觉 + 当前任务 + 旅程目标 |
| `G` | 手机闪光灯照亮全场景约 1.5 秒；光起时不能移动；冷却 8 秒 |
| `F` | 进入 / 退出全屏 |
| `Esc` | 退出全屏、暂停游戏，或关闭当前弹窗 |
| 对话时 `W/S` 或 `方向键` | 上下选择回应；`E` / `Enter` 确认；`Esc` 稍后再说 |

### 3. 旅程流程

#### 开场 · 一条未读语音

- **主菜单**：选择**体验模式**或**黑夜模式**，开始新旅程或继续上次的存档。
- **开场页**：林伯发来一条**粤语语音留言**——“小闻，雨小了。我在大三巴老地方等你。慢慢来，听到牌坊下的雨声就给我消息。” 你会看到一张旧合照，并选择一条回复。
- **出发前教程**：认识两种盲道（**四条凸纹**＝继续前进；**4×4 凸点**＝停下探测并改变方向），试听一次盲杖敲击，然后从拱北口岸门口出发。

#### 第一站 · 拱北口岸（關閘 · 17路候车区）

1. 一名**口岸工作人员**走近询问是否需要引导。你可以接受扶盲说明（解锁「扶盲方法」贴士）、只要方向，或礼貌拒绝——三种选择都保留正常路线。
2. 沿**四纹盲道**找到 17 路站牌：用盲杖辨认牌面凸字 **17**（沿途有一块相似的 **25** 号站牌）。
3. 按 `R` 聆听，确认**17 路巴士的引擎声**方向。
4. 触觉与声音位置一致后，组合确认完成，**17 路车门开启**，靠近按 `E` 上车（解锁「帮助盲人乘车」贴士）。

#### 第二站 · 17路车厢（无连续盲道）

1. 车厢内**没有连续盲道**，需要自由摸索：用 `Space` 找到**刷卡机**，按 `R` 听**短促电子音**，位置一致后按 `E` 刷卡。
2. 找到**空座**坐下。你可以向**同车乘客**询问刷卡机与空座位置，或请他在到白鸽巢时提醒你。
3. **白鸽巢报站**后，在限定时间内找到**下车按铃**。按铃本身没有时间惩罚，从容确认位置后再按 `E`（解锁「公交无障碍」贴士）。
4. 按铃后**直达白鸽巢旧城**——本作没有漫长的车程过场。

#### 第三站 · 白鸽巢 · 旧城街市

1. 下车后沿盲道北行到**路缘点阵**，用 `Space` 确认路缘、按 `R` 听**过街信号双音**，一致后按 `E` **请求通行**。
2. 留在路缘等候约 2.5 秒，收到**文字 + 双音**提示后，沿**斑马线向东直行**到对岸。
3. 沿盲道向南进入**商铺街**。街口点阵分出**两条同样安全、最终合流的走法**：
   - **店铺墙侧**：沿墙脚与店门风铃前进；
   - **路缘排水侧**：沿路缘与排水金属纹前进。
4. 途经**蛋挞摊位**：走进摊前香气区会提示“你被蛋挞的香气环绕”；买一个刚出炉的蛋挞，暖意化作**持续 60 秒的 +60% 移速**。
5. 在**猫记宠物店**前停下，向店员了解**导盲犬在澳门的处境**（解锁「导盲犬在澳门」贴士），收获一段知识性的停顿。
6. 沿盲道北上，**银号门前**就是旧城街口终点，前方通向大三巴。

#### 终章 · 大三巴牌坊

1. 沿坡道找到**轮椅上的林伯**与推行他的**女儿**：用 `Space` 确认轮椅脚踏板、按 `R` 听**轮椅轻响**，一致后按 `E` 问候。
2. 重逢时有**三个同等成功的选择**：**一起重拍旧照片**、**先听牌坊前的雨声**、**把一路想起的往事讲给林伯听**。
3. 女儿稳稳推着林伯沿中央坡道上行，你紧随其后（解锁「轮椅推行需要学习」贴士）。
4. 抵达牌坊平台后，你一路触碰过的**暖色记忆**依次亮起，旅程以三选一结局收束。

### 4. 核心玩法要点

- **盲杖敲击读材质**：不同材质有不同触觉音与描述（盲道凸纹、金属立柱、座椅软垫、路缘、碎土、斑马线涂装、粗糙沥青等）。
- **驻足聆听辨方向**：`R` 报告最多 3 个最近声音的**方位（左/右/前/后）与远近（很近/稍远/远处）**，例如“右侧稍远：17路巴士低沉的引擎声”。
- **触觉 + 声音组合线索（4 处）**：17 路站牌、刷卡机、路口过街信号、林伯轮椅。只有**触觉与声音位置互相印证**，互动才成立——这正是低视力出行的核心体验。
- **三条记忆**：旧城风铃（商铺短巷尽头）、暖掌葡挞（蛋挞摊）、牌坊雨声（大三巴）。它们会写进终章的回放。
- **两条模式**：体验模式触碰后留下**淡彩记忆**（上限 240 点）；黑夜模式未触碰处**归于全黑**，且不能走没有亮光的地面——按 `Space` 点亮或按 `G` 照亮。

### 5. 无障碍亮点

- **全流程键盘操作**，无需鼠标、触屏或精确点击。
- **全程语音播报**（系统 TTS，中文）+ 同步字幕；所有提示均以文字呈现。
- **声音定位**（Web Audio 立体声声像）为方向判断提供听觉通道。
- **Q 只指出目标方向，不显示完整路线**——保留低视力出行的推理过程，不代走。
- **设置项**：主音量 / 环境音 / 效果音 / 对话音量、字幕大小（0.9–1.5×）、减少动态效果、黑夜模式。
- **可及性说明**：当前版本**未经视障人士实测**，不代表真实失明体验，也不构成无障碍认证。

---

## 下篇 · 给开发

### 1. 场景流转与状态机

```
menu → opening → tutorial → playing → ending（弹层）
playing: bus-stop（關閘） → bus-interior（17路车厢） → old-city（白鸽巢旧城） → ruins（大三巴牌坊）
```

- 场景切换时显示**章节过场**（2.5 秒，任意键跳过）。
- **按铃直达旧城**：`oldCityCheckpointAfterBell` 把公交状态推进到 `arrived` 并落到 `old-city-entry`，旧城入场时完成下车播音——本作**没有 bus-ride 车程场景**。

**公交状态机** `transitionBus`（`src/game/flow.ts`）：

```
waiting → doorOpen → boarding → seated → riding → arrived → alighted
```

**过街状态机** `transitionCrossing`：

```
approach → requested → walk → crossed
```

请求后等待 `OLD_CITY_CROSSING.waitMs = 2500` 才放行；`corridorWidth = 48` 约束斑马线走廊。

**结局判定** `determineEnding`：

```
returnRequested ? "return"
: detourScore >= 5 ? "detour"
: "reunion"
```

`return` 需在暂停菜单主动“请求帮助并结束旅程”；`detourScore` 来自上机动车道的危险纠偏次数；走完正常流程即 `reunion`，三个重逢选择**不分高下**。

### 2. Objective 序列表（`src/game/content.ts`）

| 场景 | 目标 id（按顺序） | 说明 |
| --- | --- | --- |
| bus-stop | `find-stop-sign` → `board-17` | 触摸凸字17站牌 + 听引擎声组合确认 → 上车 |
| bus-interior | `find-card-reader` → `find-seat` → `ring-bell` | 刷卡机（触觉+电子音）→ 空座 → 按铃直达旧城 |
| old-city | `request-crossing` → `wait-crossing` → `cross-junction` → `follow-street-south` →（二选一 `follow-shop-wall` / `follow-curb-edge`）→ `follow-street-east` → `visit-pet-shop` → `reach-terminus` | 过街 → 商铺街 → 两条合流走法 → 宠物店知识停顿 → 街口终点 |
| ruins | `meet-lam` → `follow-wheelchair` | 组合确认轮椅林伯 → 紧随队伍上行 |

**检查点**（`CHECKPOINTS`，`src/game/flow.ts`，共 11 个 `resumeStage`）：
`bus-stop-entry`、`bus-stop-sign`、`bus-interior-entry`、`bus-interior-seat`、`bus-interior-bell`、`old-city-entry`、`old-city-wait`、`old-city-go`、`old-city-street`、`ruins-entry`、`ruins-procession`。

### 3. 数据表

**三条记忆**（`MEMORY_DEFINITIONS`，`src/game/journey.ts`）：

| id | 标题 | 触发 |
| --- | --- | --- |
| `old-city-bell` | 旧城风铃 | 商铺短巷死胡同（`OLD_CITY_MEMORY_POINT`） |
| `egg-tart` | 暖掌葡挞 | 在蛋挞摊购买 |
| `ruins-rain` | 牌坊雨声 | 大三巴按 `R` 听到 `ruins-rain` 声景 |

**五条公益贴士**（`TIP_DEFINITIONS`，`src/game/content.ts`，HUD 计数 记忆 n/3 · 贴士 n/5）：

| id | 标题 | 解锁源 |
| --- | --- | --- |
| `sighted-guide` | 扶盲方法 | 口岸工作人员（开场 intro） |
| `bus-access` | 帮助盲人乘车 | 17 路车门（bus-door） |
| `bus-ride-access` | 公交无障碍 | 车厢按铃（bell） |
| `wheelchair-pushing` | 轮椅推行需要学习 | 大三巴重逢（wheelchair） |
| `guide-dog-access` | 导盲犬在澳门 | 猫记宠物店（pet-shop） |

**三个结局**（`ENDING_COPY`，`src/App.tsx`）：`reunion` 如期而至 / `detour` 雨巷绕行 / `return` 迷途折返；结局结算显示 记忆 / 危险纠偏 / 旧城走法。

**蛋挞参数**（`src/game/egg-tart.ts`）：摊位 `(344,260)`；香气区为摊前 **6 列 × 3 行** 16px 格（`x 312..392, y 268..308`），首次进入一次性提示“你被蛋挞的香气环绕。”；购买一次后获得 `60_000ms` 内 `×1.6` 移速加成，**跨场景、跨存档保留，不叠加、不刷新**；对话、暂停与过场不计时（`boostClockFrozen`）。

**移动速度公式**（`src/game/flow.ts`）：

```
speed = BASE_WALK_SPEED(68) × terrainMultiplier × boostMultiplier
```

| 情形 | 系数 |
| --- | --- |
| 室外盲道上（`onGuidedPath`） | `1.00` → 68 px/s |
| 室外离开盲道（无连续凸纹） | `0.35` → 38.08 px/s |
| 无盲道车厢（bus-interior，无 path） | `1.00` |
| 蛋挞加成期间 | 再 × `1.60` → 盲道上 108.8 px/s |

**声音地标**（`SOUND_LANDMARKS`，`src/game/sound-landmarks.ts`，共 11 个）：`R` 聆听返回最近 3 个、报告方位（左/右/正前/身后）与距离带（很近 ≤64 / 稍远 ≤140 / 远处）。四个组合线索对应：`route-17-engine`、`bus-card-reader`、`old-city-crossing`、`ruins-wheelchair`。

### 4. 存档与兼容（`src/game/store.ts`）

- 存档键：`SAVE_KEY = "sound-road-macau-2d:v5"`；旧键 `v4` 读取后迁移并移除，`v3/v2` 直接清理。
- `GameSnapshotV5` 核心字段：`version / scene / objectiveId / resumeStage / busState / memories / detourScore / activeElapsedMs / returnRequested / ending / colorMemory / openingReply / endingChoice / routeChoice / npcChoices / eggTartPurchased / eggTartBoostRemainingMs / eggTartScentPrompted / settings / unlockedTips / selectedSeatId`。
- **V4→V5 迁移**：读入后统一归一化场景与 resumeStage（车厢按 objective 修正 `busState`；旧城/候车/终点按 objective 修复阶段），未知枚举值直接退化为全新开始而非崩溃。
- **设置项**（默认值）：`masterVolume 0.72 / ambientVolume 0.58 / effectsVolume 0.8 / dialogueVolume 0.9 / subtitleScale 1 / reducedMotion false / gameMode "experience"`。

### 5. 技术栈与验证

| 层 | 技术 |
| --- | --- |
| 游戏引擎 | Phaser 3.90.0（640×360 canvas，16px 瓦片） |
| 界面外壳 | React 19.2 + TypeScript 5.9 |
| 构建 | Vite 6.4 + `vite-plugin-singlefile` |
| 测试 | Vitest 3.2 —— **107 项单测全部通过**（16 个测试文件） |
| 产物 | `dist/`（标准）、`offline-dist/index.html`（单文件离线，`npm run build:offline`）、`dist/client` + `dist/server` + hosting.json（`build:sites`，`test:sites` 4/4） |

浏览器验证钩子：`window.render_game_to_text()`（结构化状态快照）与 `window.advanceTime(ms)`（确定性推进）。

### 6. 版本基线提示

- 本文件只描述**当前游戏**。历史上曾存在的**17路途中车程过场（bus-ride）**、**声音地标收集系统（knownLandmarks / HUD 声音地标计数）**、**bus-rain / border-hand 记忆**、**旧城独立路口场景**与**扶手机制**均已在 2026-08-06 移除或合并，`docs/q-align/agent-exec-plan.md`、`design-qa.md` 等早期文档中的相关描述**不可再作为事实来源**。
